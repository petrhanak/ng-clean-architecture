{
  "name": "web-e2e",
  "version": "0.0.0",
  "scripts": {
    "start": "cypress open",
    "cli": "cypress run",
    "ci": "start-server-and-test 'yarn workspace web run start' http://localhost:4200 cli"
  },
  "private": true,
  "devDependencies": {
    "cypress": "^6.6.0",
    "start-server-and-test": "^1.12.0",
    "typescript": "~4.1.5"
  }
}
